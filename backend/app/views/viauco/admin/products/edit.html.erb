<% content_for :page_actions do %>
  <% if frontend_available? %>
    <%= button_link_to Viauco.t(:preview_product), product_url(@product), { class: "btn-outline-secondary", icon: 'eye-open', id: 'admin_preview_product', target: :_blank } %>
  <% end %>
  <% if can?(:create, Viauco::Product) %>
    <%= button_link_to Viauco.t(:new_product), new_object_url, { class: "btn-success", icon: 'add', id: 'admin_new_product' } %>
  <% end %>
<% end %>

<%= render partial: 'viauco/admin/shared/product_tabs', locals: {current: :details} %>
<%= render partial: 'viauco/admin/shared/error_messages', locals: { target: @product } %>

<%= form_for [:admin, @product], method: :put, html: { multipart: true } do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'viauco/admin/shared/edit_resource_links' %>
  </fieldset>
<% end %>
