<% if can?(:admin, current_store) && Viauco::Config[:admin_show_version] %>
  <small
    class="viauco-version <%= 'd-md-block' if cookies['sidebar-minimized'] == 'false' %> d-none font-weight-bold ml-3 my-3 text-muted"
    aria-label="Viauco Commerce version">
    <%= Viauco.version %>
  </small>
<% end %>
