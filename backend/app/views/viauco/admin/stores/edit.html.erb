<% content_for :page_title do %>
  <%= link_to Viauco.t(:stores), viauco.admin_stores_url %> /
  <%= @store.name %>
<% end %>

<%= render partial: 'viauco/admin/shared/error_messages', locals: { target: @store } %>

<%= form_for [:admin, @store] do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'viauco/admin/shared/edit_resource_links', locals: { collection_url: viauco.admin_stores_path(@store) } %>
  </fieldset>
<% end %>
