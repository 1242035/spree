<% address_id = address_type.chars.first %>

<% Viauco::Address::ADDRESS_FIELDS.each do |field| %>
  <% if field == "country" %>
    <div class="form-group" id="<%= "#{address_id}country" %>">
      <%= address_form.label :country_id, Viauco.t(:country) %><span class="required">*</span><br />
      <span id="<%= "#{address_id}country-selection" %>">
        <%= address_form.collection_select :country_id, available_countries, :id, :name, {}, {:class => 'required form-control'} %>
      </span>
    </div>
  <% elsif field == "state" %>
    <%= address_field(address_form, :state, address_id) { address_state(address_form, address.country, address_id)} if Viauco::Config[:address_requires_state] %>
  <% else %>
    <% next if field == "company" && !Viauco::Config[:company] %>
    <% next if field == "alternative_#{address_id}_phone" && !Viauco::Config["alternative_#{address_id}_phone"] %>
    <%= address_field(address_form, field.to_sym, address_id) %>
  <% end %>
<% end %>
